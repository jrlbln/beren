export default function QuizPage() {
  return (
    <section className="rounded-[2rem] border border-dashed border-slate-300 bg-white/85 p-8 shadow-sm">
      <p className="text-sm font-semibold uppercase tracking-[0.24em] text-amber-600">
        Phase 3
      </p>
      <h1 className="mt-3 text-3xl font-semibold tracking-tight text-slate-950">
        Quiz tab is reserved for the quiz engine phase
      </h1>
      <p className="mt-4 max-w-2xl text-sm leading-7 text-slate-600">
        The route is available now so navigation matches the planned app
        structure, but quiz session logic remains out of scope for Phase 1.
      </p>
    </section>
  );
}
